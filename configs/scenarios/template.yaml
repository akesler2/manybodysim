# Scenario overrides for a specific run.
# Only include values that differ from configs/base.yaml.

world:
  n_particles: 250
  t_end: 15.0

particle_init:
  v_mean: 8.0
  v_std: 3.0

numerics:
  dt: 0.005
  integrator: euler

physics:
  force_model: none

# Example: dimensionless toy gravity (override defaults from base.yaml)
# physical_constants:
#   gravitational_constant: 1.0
